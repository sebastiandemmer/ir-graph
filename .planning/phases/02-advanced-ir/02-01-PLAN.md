---
phase: 02-advanced-ir
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, src/irgraph/Graph.py, src/http_app/routes/api/graphs.py, src/mcp_server.py, src/mcp_client.py, tests/unit/test_blast_radius.py]
autonomous: true
requirements: [R4.1, R4.2]
must_haves:
  truths:
    - "NetworkX is available in the environment"
    - "Graph object can be converted to NetworkX DiGraph"
    - "MCP tool 'get_blast_radius' returns correct reachable nodes"
  artifacts:
    - path: "src/irgraph/Graph.py"
      provides: "to_networkx() method"
    - path: "src/mcp_server.py"
      provides: "get_blast_radius tool"
  key_links:
    - from: "src/mcp_server.py"
      to: "src/mcp_client.py"
      via: "client.get_blast_radius"
---

<objective>
Integrate NetworkX and implement the `get_blast_radius` analysis tool to identify reachable nodes from a starting point (e.g., infected host).
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/irgraph/Graph.py
@src/mcp_server.py
@src/http_app/routes/api/graphs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate NetworkX and Graph Conversion</name>
  <files>pyproject.toml, src/irgraph/Graph.py</files>
  <action>
    1. Add `networkx` to the `dependencies` list in `pyproject.toml`.
    2. Implement `to_networkx(self) -> networkx.DiGraph` in `src/irgraph/Graph.py`.
    3. The conversion should map nodes to NetworkX nodes and edges to NetworkX directed edges.
  </action>
  <verify>
    <automated>python3 -c "import networkx; from irgraph.Graph import Graph; g = Graph('test'); print('OK')"</automated>
  </verify>
  <done>NetworkX is a dependency and Graph objects can be converted to DiGraphs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Blast Radius API and Client</name>
  <files>src/http_app/routes/api/graphs.py, src/mcp_client.py, tests/unit/test_blast_radius.py</files>
  <behavior>
    - Given a graph with A -> B -> C and D.
    - get_blast_radius(A) should return [B, C].
    - get_blast_radius(D) should return [].
  </behavior>
  <action>
    1. Add `GET /api/graphs/{graph_id}/blast-radius/{node_name}` to `src/http_app/routes/api/graphs.py`.
    2. Use `graph.to_networkx()` and `networkx.descendants(G, node_name)` to calculate the blast radius.
    3. Add `get_blast_radius(graph_id, node_name)` to `src/mcp_client.py`.
    4. Create unit tests in `tests/unit/test_blast_radius.py`.
  </action>
  <verify>
    <automated>pytest tests/unit/test_blast_radius.py</automated>
  </verify>
  <done>API endpoint and client method return correct reachability data.</done>
</task>

<task type="auto">
  <name>Task 3: Expose Blast Radius Tool in MCP Server</name>
  <files>src/mcp_server.py</files>
  <action>
    1. Add `@mcp.tool()` `get_blast_radius(graph_id: int, node_name: str) -> str`.
    2. Call `client.get_blast_radius(graph_id, node_name)`.
    3. Return a formatted string or JSON listing the reachable nodes.
  </action>
  <verify>
    <automated>python3 src/mcp_server.py --help | grep get_blast_radius</automated>
  </verify>
  <done>The MCP server exposes the get_blast_radius tool.</done>
</task>

</tasks>

<success_criteria>
- `networkx` is installed.
- `get_blast_radius` tool is available via MCP.
- AI agents can identify all downstream nodes affected by a compromised node.
</success_criteria>
