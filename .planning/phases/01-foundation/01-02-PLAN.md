---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/mcp_server.py]
autonomous: true
requirements: [R2.1, R2.2]

must_haves:
  truths:
    - "FastMCP server is initialized and named 'IR-Graph'."
    - "Resource graph://{graph_id} returns the full state of the specified graph."
  artifacts:
    - path: "src/mcp_server.py"
      provides: "FastMCP server instance"
      exports: ["mcp"]
  key_links:
    - from: "src/mcp_server.py"
      to: "src/mcp_client.py"
      via: "import IRGraphClient"
      pattern: "from src\.mcp_client import IRGraphClient"
---

<objective>
Initialize the MCP server using the FastMCP framework and implement the primary read-only resource for graph state inspection.

Purpose: Provide agents with a direct resource to read current graph state.
Output: Initialized MCP server with one registered resource.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/1-CONTEXT.md
@.planning/1-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize FastMCP Server</name>
  <files>src/mcp_server.py</files>
  <action>
    Create `src/mcp_server.py`.
    Initialize a `FastMCP` instance named "IR-Graph".
    Ensure it's configured for the `mcp` library as recommended in RESEARCH.md.
  </action>
  <verify>
    <automated>grep -q "FastMCP("IR-Graph")" src/mcp_server.py</automated>
  </verify>
  <done>`FastMCP` instance created in `src/mcp_server.py`.</done>
</task>

<task type="auto">
  <name>Task 2: Implement graph:// resource</name>
  <files>src/mcp_server.py</files>
  <action>
    Add a resource decorator to `src/mcp_server.py` to handle `graph://{graph_id}`.
    Use `IRGraphClient` to fetch the graph state from the API.
    Return the full graph JSON as a string.
  </action>
  <verify>
    <automated>grep -q "@mcp.resource("graph://{graph_id}")" src/mcp_server.py</automated>
  </verify>
  <done>Resource `graph://` implemented and calling the API client.</done>
</task>

</tasks>

<success_criteria>
- `src/mcp_server.py` exists with a named `FastMCP` instance.
- Resource `graph://{graph_id}` is correctly decorated and calls the `IRGraphClient`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
