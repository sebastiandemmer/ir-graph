---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified: [src/mcp_server.py, src/http_app/__init__.py]
autonomous: true
requirements: [R3.1, R3.2]

must_haves:
  truths:
    - "Standalone CLI supports --api-url flag."
    - "Logging is correctly configured to file and stderr, preserving stdout for MCP."
    - "MCP server is mounted and accessible via SSE in the FastAPI application."
  artifacts:
    - path: "mcp_server.log"
      provides: "Server-side logs"
  key_links:
    - from: "src/http_app/__init__.py"
      to: "src/mcp_server.py"
      via: "sse_app() mounting"
      pattern: "app\.mount\(.*mcp\.sse_app\(\)\)"
---

<objective>
Enable multiple transport options for the MCP server by implementing a standalone stdio CLI and mounting the server as an SSE application within the existing FastAPI web app.

Purpose: Provide flexibility for agents to connect via desktop stdio or web-based SSE.
Output: CLI entry point and integrated SSE endpoint.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/1-CONTEXT.md
@.planning/1-RESEARCH.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standalone CLI Entry Point</name>
  <files>src/mcp_server.py</files>
  <action>
    Implement an `if __name__ == "__main__":` block in `src/mcp_server.py`.
    Use `argparse` to support the `--api-url` flag (defaulting to `http://localhost:8000`).
    Configure logging using the standard library `logging` to output to `mcp_server.log` and `sys.stderr`.
    Call `mcp.run()` to start the server in stdio mode.
  </action>
  <verify>
    <automated>python src/mcp_server.py --help | grep -q "--api-url"</automated>
  </verify>
  <done>CLI entry point implemented with correct logging and flag support.</done>
</task>

<task type="auto">
  <name>Task 2: FastAPI Mounting (SSE Transport)</name>
  <files>src/http_app/__init__.py</files>
  <action>
    Import the `mcp` instance from `src.mcp_server` into `src/http_app/__init__.py`.
    Mount the MCP SSE application using `app.mount("/mcp", mcp.sse_app())`.
    Ensure this mounting only occurs if the main FastAPI app is successfully created.
  </action>
  <verify>
    <automated>grep -q "app.mount("/mcp"" src/http_app/__init__.py</automated>
  </verify>
  <done>MCP server mounted as an SSE app at `/mcp`.</done>
</task>

</tasks>

<success_criteria>
- Running `python src/mcp_server.py` starts an MCP server on stdio.
- `GET /mcp` (or similar SSE handshake) on the web server returns an MCP-specific response.
- `stdout` is free of non-MCP logs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
