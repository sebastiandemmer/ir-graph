---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [src/mcp_server.py]
autonomous: true
requirements: [R2.3]

must_haves:
  truths:
    - "Tools 'create_graph', 'add_node', 'add_edge', 'update_node', 'delete_node' are registered."
    - "Agent can create new graphs (which are automatically named with '(agent)')."
    - "Agent can ONLY modify nodes/edges in graphs with '(agent)' in their name."
    - "Meaningful error messages are returned when guardrails fail."
  artifacts:
    - path: "src/mcp_server.py"
      provides: "CRUD tools for graph modification"
  key_links:
    - from: "src/mcp_server.py"
      to: "src/mcp_client.py"
      via: "is_agent_owned() calls"
      pattern: "if not await client\.is_agent_owned\(graph_id\):"
---

<objective>
Implement the core CRUD tools for graph manipulation, ensuring that agent-specific guardrails are strictly enforced to prevent unauthorized modification of human-owned graphs.

Purpose: Enable agents to create and modify graphs safely while maintaining strict ownership boundaries.
Output: Registered MCP tools for graph creation and mutation with guardrail logic.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/1-CONTEXT.md
@.planning/1-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement create_graph, add_node and add_edge</name>
  <files>src/mcp_server.py</files>
  <action>
    Add `create_graph`, `add_node` and `add_edge` tools to `src/mcp_server.py`.
    - `create_graph(name: str)`: Calls `IRGraphClient.create_graph(name)`. The client should handle appending "(agent)".
    - `add_node(graph_id: int, ...)` and `add_edge(graph_id: int, ...)`: Before each mutation, use `IRGraphClient.is_agent_owned(graph_id)` to verify the graph is agent-owned.
    If not agent-owned, return a "Permission Denied" message as specified in Dec-1 of CONTEXT.md.
    If agent-owned, call the appropriate FastAPI endpoint via the client.
  </action>
  <verify>
    <automated>grep -E "def (create_graph|add_node|add_edge)" src/mcp_server.py</automated>
  </verify>
  <done>`create_graph`, `add_node` and `add_edge` implemented with ownership checks.</done>
</task>

<task type="auto">
  <name>Task 2: Implement update_node and delete_node</name>
  <files>src/mcp_server.py</files>
  <action>
    Add `update_node` and `delete_node` tools to `src/mcp_server.py`.
    Enforce the same `(agent)` ownership guardrails as in Task 1: verify ownership using `is_agent_owned(graph_id)` before calling the client's mutation methods.
    Map these tools to the existing FastAPI endpoints in `src/http_app/routes/api/graphs.py` via the `IRGraphClient`.
  </action>
  <verify>
    <automated>grep -E "def (update|delete)_node" src/mcp_server.py</automated>
  </verify>
  <done>`update_node` and `delete_node` implemented with ownership checks.</done>
</task>

</tasks>

<success_criteria>
- All 5 CRUD tools are registered with `FastMCP`.
- `create_graph` results in a graph with `(agent)` in its name.
- Attempting to modify a graph without `(agent)` in its name fails with a permission error.
- Successful modification of `(agent)` graphs is reflected in the backend state.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
