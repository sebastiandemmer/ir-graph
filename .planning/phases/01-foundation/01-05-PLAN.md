---
phase: 01-foundation
plan: 05
type: execute
wave: 5
depends_on: [01-04]
files_modified: []
autonomous: false
requirements: [R3.1, R3.2]

must_haves:
  truths:
    - "MCP server returns a clear error message when the FastAPI backend is down."
    - "Claude Desktop can connect to the MCP server and perform graph mutations."
    - "Guardrails correctly prevent modification of non-agent graphs."
---

<objective>
Verify the end-to-end functionality of the MCP server through robust manual and automated tests, covering both error conditions and successful integration with AI agents.

Purpose: Ensure the system is reliable and safe for production-like use.
Output: Verification report and a working MCP integration.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/1-CONTEXT.md
@.planning/1-RESEARCH.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify 'API Unreachable' Error</name>
  <files>None</files>
  <action>
    Stop the FastAPI web server.
    Attempt to invoke the MCP server using the stdio transport (e.g., using `mcp-inspector` or a manual stdio script).
    Verify that it returns the specific error message: "Error: IR Graph API unreachable at http://localhost:8000. Please ensure the web server is running."
  </action>
  <verify>
    <automated>python -c "import subprocess, sys; p = subprocess.Popen(['python', 'src/mcp_server.py'], stdin=subprocess.PIPE, stdout=subprocess.PIPE, stderr=subprocess.PIPE, text=True); out, err = p.communicate(input='{"jsonrpc": "2.0", "id": 1, "method": "resources/read", "params": {"uri": "graph://1"}}'); print(err)" | grep "IR Graph API unreachable"</automated>
  </verify>
  <done>Error handling for unreachable API verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>End-to-end MCP integration with stdio and SSE transports.</what-built>
  <how-to-verify>
    1. Start the FastAPI web server: `python src/http_app/dev_server.py`.
    2. Add the MCP server to Claude Desktop configuration (`~/Library/Application\ Support/Claude/claude_desktop_config.json`):
       ```json
       {
         "mcpServers": {
           "ir-graph": {
             "command": "python",
             "args": ["/absolute/path/to/src/mcp_server.py"]
           }
         }
       }
       ```
    3. Restart Claude Desktop.
    4. Ask Claude: "List my graphs" or "Read graph://1".
    5. Ask Claude to "create a new graph named 'Investigation'".
    6. Verify the new graph exists (it should be named "Investigation (agent)").
    7. Ask Claude to "add a node named 'Malware-01' to the investigation graph".
    8. Verify the node appears in the Web UI.
    9. Ask Claude to modify a graph without "(agent)" in its name and verify it fails with a permission error.
  </how-to-verify>
  <resume-signal>Type "approved" once integration and guardrails are verified.</resume-signal>
</task>

</tasks>

<success_criteria>
- Successful connection from Claude Desktop.
- Guardrails effectively block unauthorized modifications.
- API unreachable condition returns the user-friendly error message.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
